targets:
  $default:
    builders:
      drift_dev:
        options:
          # Храним даты как ISO-8601 текст: UTC-значения читаются обратно как UTC.
          store_date_time_values_as_text: true
          databases:
            app_database: lib/data/db/app_database.dart
          schema_dir: drift_schemas/
          test_dir: test/drift/
